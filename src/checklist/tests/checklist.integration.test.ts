import { describe, it } from "vitest";

describe("checklist integration", () => {
  it.todo("POST /api/checklist/generate returns 201 with valid profile");
  it.todo("POST /api/checklist/generate returns 400 with invalid body");
  it.todo("POST /api/checklist/evaluate returns 200 with valid input");
  it.todo("POST /api/checklist/evaluate returns 400 with missing body");
});
